Place the exported Rive file here:

ambient-mascot-placeholder.riv

Expected state machine: Mascot

Inputs:
- isHovering: boolean
- cursorX: number, normalized 0 to 1
- cursorY: number, normalized 0 to 1
- react: trigger

The component will still render its container if the file is missing, but the
Rive animation appears after this binary .riv export is added.
